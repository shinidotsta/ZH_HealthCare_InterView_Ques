import { HealthCareDetails } from './health-care-details';

describe('HealthCareDetails', () => {
  it('should create an instance', () => {
    expect(new HealthCareDetails()).toBeTruthy();
  });
});
